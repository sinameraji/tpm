---
interface Props {
  title: string;
  description?: string;
}
const { title, description = "TPM — Technical Product Manager. Audits software products via a deterministic six-stage pipeline." } = Astro.props;
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://usetpm.dev/" />
  <link rel="canonical" href="https://usetpm.dev/" />
  <style>
    :root {
      --bg: #fafaf7;
      --fg: #1a1a1a;
      --muted: #4d4d4d;
      --border: #e4e4e0;
      --brand: #0654ba;
      --brand-dark: #0345a0;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, "Segoe UI", "Helvetica Neue", sans-serif;
      line-height: 1.55;
      color: var(--fg);
      background: var(--bg);
    }
    header, main, footer {
      max-width: 64rem;
      margin: 0 auto;
      padding: 1.5rem 1.5rem;
    }
    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid var(--border);
    }
    header .brand {
      font-weight: 700;
      color: var(--brand);
      letter-spacing: 0.02em;
    }
    header nav a {
      margin-left: 1.25rem;
      color: var(--muted);
      text-decoration: none;
      font-size: 0.92rem;
    }
    header nav a:hover { color: var(--fg); }
    footer {
      border-top: 1px solid var(--border);
      color: var(--muted);
      font-size: 0.88rem;
      padding-top: 2rem;
      padding-bottom: 3rem;
    }
    footer a { color: var(--muted); }
    .button {
      display: inline-block;
      background: var(--brand);
      color: white;
      text-decoration: none;
      padding: 0.65rem 1.25rem;
      border-radius: 6px;
      font-weight: 600;
      font-size: 0.95rem;
    }
    .button:hover { background: var(--brand-dark); }
    .button.secondary {
      background: transparent;
      color: var(--brand);
      border: 1px solid var(--brand);
    }
    .button.secondary:hover { background: var(--brand); color: white; }
    code, pre {
      font-family: "SF Mono", "Menlo", monospace;
      font-size: 0.9em;
      background: #efefe9;
      border-radius: 4px;
      padding: 1px 6px;
    }
    pre {
      padding: 1rem;
      overflow-x: auto;
    }
  </style>
</head>
<body>
  <header>
    <a href="/" class="brand">TPM · Technical Product Manager</a>
    <nav>
      <a href="/pricing">Pricing</a>
      <a href="/docs">Docs</a>
      <a href="/upgrade">Upgrade</a>
    </nav>
  </header>
  <main>
    <slot />
  </main>
  <footer>
    <p>
      © 2026 TPM. <a href="/privacy">Privacy</a> · <a href="/terms">Terms</a> · <a href="mailto:support@usetpm.dev">support@usetpm.dev</a>
    </p>
  </footer>
</body>
</html>
